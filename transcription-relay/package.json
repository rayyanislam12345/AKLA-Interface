{
  "name": "transcription-relay",
  "version": "1.0.0",
  "private": true,
  "description": "WebSocket relay holding the Deepgram API key, so the browser recording page never sees it directly.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  }
}
