// Stub for Phase 0 — the old version logged logins/feature-usage against
// tables that belonged to the deleted PE product. A matter-hub-appropriate
// audit trail (logins, matter/document access) is a later-phase addition.
export function useActivityTracking() {
  // no-op
}
