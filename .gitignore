# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# bun.lock is canonical; npm regenerates this if run directly
package-lock.json

# QA bot run artifacts
qa-bot/videos/
qa-bot/screenshots/
qa-bot/traces/

# Secrets
.env
.env.local
.env.*.local
.mcp.json

# Mandate Bot lives in its own separate git repo (AKLA_Mandate_Bot) —
# never let it get staged into this one as a gitlink.
Mandate Bot/

# whatsapp-dashboard is a separate standalone project, not yet under any
# git repo, holding real API keys (.env), real client data (data/), and
# live WhatsApp session tokens (.wppconnect_auth/, .wwebjs_auth/) —
# never let this get staged into this repo.
whatsapp-dashboard/

# Precedent backlog ingestion tool: run-local artifacts, not source
scripts/precedent_backlog/manifest.json
scripts/precedent_backlog/report.json
scripts/precedent_backlog/.venv/
__pycache__/
*.pyc

# Law library scraper: downloaded PDFs and run-local state, not source
scripts/law_library/manifest.json
scripts/law_library/downloads/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
