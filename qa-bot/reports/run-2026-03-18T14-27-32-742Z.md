# FactorIQ QA Report
**Date:** 2026-03-18T14:27:32.744Z
**Target:** http://localhost:8081

I'll start the QA test systematically, thinking through each phase. Let me begin by navigating to the application.
> navigate({"url":"http://localhost:8081"})

> screenshot({})
The app isn't up yet. Let me wait a moment and retry.
> wait({"ms":3000})

> navigate({"url":"http://localhost:8081"})

> wait({"ms":5000})

> navigate({"url":"http://localhost:8081"})
Let me check if the app might be on a different port, and also try to read any config files to understand the setup.
> navigate({"url":"http://localhost:3000"})

> read_file({"path":"package.json"})

> read_file({"path":"vite.config.ts"})
The Vite config shows port 8080. Let me try that port.
> navigate({"url":"http://localhost:8080"})

> screenshot({})
The app is on port 8080! Now let me navigate to the sign-in page.
> click({"text":"Sign In"})

> screenshot({})
Good — the auth page is up. Let me sign in with the provided credentials.
> type({"selector":"input[type=\"email\"]","text":"recognizedemo100@gmail.com"})

> type({"selector":"input[type=\"password\"]","text":"factorIQ100"})

> click({"text":"Sign In"})

> wait({"ms":3000})

> screenshot({})
Still on auth page — let me check if there's an error message and inspect the form more carefully.
> get_page_content({})

> eval({"code":"document.querySelector('form') ? document.querySelector('form').innerHTML : 'no form found'"})
I see the form uses specific IDs. Let me fill them properly and check for any toast/error messages after submit.
> eval({"code":"\n// Check current field values\nconst email = document.getElementById('signin-email');\nconst pass = document.getElementById('signin-password');\nreturn JSON.stringify({ emailVal: email?.value, passVal: pass?.value });\n"})

> eval({"code":"\nconst email = document.getElementById('signin-email');\nconst pass = document.getElementById('signin-password');\nJSON.stringify({ emailVal: email?.value, passVal: pass?.value ? '***' : '' });\n"})
Good — credentials are filled. Let me submit the form and monitor for errors.
> eval({"code":"document.querySelector('button[type=\"submit\"]').click(); 'clicked';"})

> wait({"ms":4000})

> screenshot({})
✅ **PHASE 1 PASS — Sign-in successful!** The app logged in and redirected to `/home`. I can see KPIs (1 Fund, 12 Investments, 3 Deals, 0 Projects, 5 Recent Uploads) and a Portfolio News Summary. Let me explore the dashboard more thoroughly.
> get_page_content({})

> screenshot({})
Good. Now let me look for the navigation menu to understand the full structure of the app.
> eval({"code":"\n// Get all navigation links\nconst links = Array.from(document.querySelectorAll('a, nav button, [role=\"navigation\"] button'));\nlinks.map(l => ({ text: l.innerText.trim(), href: l.href || 'button' })).filter(l => l.text).slice(0, 30);\n"})
Excellent! I can see the navigation structure. Now let me look for the sidebar navigation and check for a sidebar menu.
> eval({"code":"\n// Find sidebar nav elements\nconst sidebar = document.querySelector('aside, [class*=\"sidebar\"], nav');\nsidebar ? sidebar.innerText.trim().substring(0, 500) : 'no sidebar found';\n"})

> eval({"code":"\n// Look for any left sidebar / app shell nav\nconst allNavLinks = Array.from(document.querySelectorAll('aside a, [data-sidebar] a, .sidebar a'));\nallNavLinks.map(l => ({ text: l.innerText.trim(), href: l.href })).filter(l => l.text);\n"})

> eval({"code":"\n// Try finding any hamburger or sidebar toggle\nconst triggers = Array.from(document.querySelectorAll('[class*=\"trigger\"], [class*=\"menu\"], [class*=\"nav\"], button'));\ntriggers.map(t => t.innerText.trim()).filter(Boolean).slice(0, 20);\n"})

> eval({"code":"\n// Find the user avatar / profile icon in top left (shows \"RE\")\nconst avatars = Array.from(document.querySelectorAll('[class*=\"avatar\"], [class*=\"Avatar\"]'));\navatars.map(a => a.outerHTML.substring(0, 200));\n"})

> eval({"code":"\n// The \"RE\" initials element – find it\nconst re = Array.from(document.querySelectorAll('*')).find(el => el.innerText?.trim() === 'RE' && el.children.length === 0);\nre ? re.outerHTML : 'not found';\n"})
Now let me navigate to the Data Ingestion section to test portfolio CSV upload (Phase 3).
> navigate({"url":"http://localhost:8080/data-ingestion"})

> screenshot({})
The Data Catalog is showing. I can see existing uploads. Now let me find the Upload Files button to test CSV upload.
> click({"text":"Upload Files"})

> screenshot({})
Good — I can see the upload interface. Now let me create the CSV data and upload it. First, I need to create a CSV file via JavaScript and simulate the file upload.
> eval({"code":"\n// Check what entity types are available\nconst select = document.querySelector('select, [class*=\"select\"]');\nconst options = Array.from(document.querySelectorAll('[role=\"option\"], option'));\noptions.map(o => o.innerText.trim());\n"})

> eval({"code":"\n// Find all select triggers (Radix UI uses buttons for selects)\nconst triggers = Array.from(document.querySelectorAll('[role=\"combobox\"], button[aria-haspopup]'));\ntriggers.map(t => ({ text: t.innerText.trim(), ariaLabel: t.getAttribute('aria-label'), id: t.id }));\n"})

> click({"selector":"[role=\"combobox\"]"})

> screenshot({})
The dropdown is open showing entity types. I'll select "Fund" as Apex Capital Partners is a fund.
> click({"text":"Fund"})

> screenshot({})
Good — now I need to select a specific Fund entity. Let me click the "Select Entity" dropdown.
> eval({"code":"\nconst combos = Array.from(document.querySelectorAll('[role=\"combobox\"]'));\ncombos.map(c => c.innerText.trim());\n"})

> eval({"code":"\nconst combos = Array.from(document.querySelectorAll('[role=\"combobox\"]'));\ncombos[1].click(); 'clicked entity selector';\n"})

> screenshot({})
I can see "Recognize Growth Fund I" is the available fund. Let me select it.
> click({"text":"Recognize Growth Fund I"})

> screenshot({})
Now I need to select a Data Type. Let me click that dropdown.
> eval({"code":"\nconst combos = Array.from(document.querySelectorAll('[role=\"combobox\"]'));\ncombos[2].click(); 'clicked data type selector';\n"})

> screenshot({})
I'll select "Fund Investments" since our CSV contains portfolio company data.
> click({"text":"Fund Investments"})

> screenshot({})
Now I need to create the CSV file and upload it. Let me use JavaScript to create a Blob/File object and programmatically attach it to the file input.
> eval({"code":"\n// Create CSV content\nconst csvContent = `company_name,fund,acquisition_year,revenue_millions,ebitda_millions,sector,status\nApex Industrial Holdings,Apex Capital Fund III,2019,125.4,22.1,Industrials,Active\nMidWest Fasteners Corp,Apex Capital Fund III,2022,48.0,9.5,Industrials,Active\nPrecision Parts Inc,Apex Capital Fund III,2020,87.3,15.6,Manufacturing,Active\nConveyor Systems Ltd,Apex Capital Fund III,2021,63.2,11.4,Industrials,Active\nSteel Fabricators Group,Apex Capital Fund III,2018,210.5,38.9,Metals,Realized`;\n\n// Create a File object\nconst blob = new Blob([csvContent], { type: 'text/csv' });\nconst file = new File([blob], 'apex_portfolio_companies.csv', { type: 'text/csv' });\n\n// Find file input\nconst fileInput = document.querySelector('input[type=\"file\"]');\nif (fileInput) {\n  const dt = new DataTransfer();\n  dt.items.add(file);\n  fileInput.files = dt.files;\n  fileInput.dispatchEvent(new Event('change', { bubbles: true }));\n  'File attached successfully';\n} else {\n  'No file input found';\n}\n"})

> wait({"ms":2000})

> screenshot({})
Excellent! The CSV was accepted and analyzed! The system detected 7 columns, 5 data rows, annual period, and the file is ready to upload. Now let me click "Standardise and Upload".
> click({"text":"Standardise and Upload"})

> wait({"ms":3000})

> screenshot({})
The column mapping review is showing. It's flagging a missing required column `period_date` and skipping most custom columns. This is expected behavior — the system wants financial period data. Let me note this and proceed, clicking "Next" to see what happens.
> click({"text":"Next"})
⚠️ The "Next" button is disabled because `period_date` is a required column. This is a validation guard. Let me note this as a UX warning — the system needs `period_date` in the CSV. Let me close this and proceed to Phase 4 (Deal Creation). This is actually correct behavior, not a bug.

Now let me navigate to the Deal Manager to create a deal for MidWest Fasteners Corp.
> click({"text":"Close"})

> navigate({"url":"http://localhost:8080/deal-manager"})

> screenshot({})
Interesting — "MidWest Fasteners Corp" already exists in the system! There are two entries. Let me click "New Deal" to test the deal creation flow as if I were onboarding fresh.
> click({"text":"New Deal"})

> screenshot({})
The "Create New Deal" modal is open. Let me fill in the details for MidWest Fasteners Corp.
> eval({"code":"\n// Find all inputs in the modal\nconst modal = document.querySelector('[role=\"dialog\"]');\nif (modal) {\n  const inputs = Array.from(modal.querySelectorAll('input, textarea, select, [role=\"combobox\"]'));\n  inputs.map(i => ({ tag: i.tagName, type: i.type, placeholder: i.placeholder, id: i.id, name: i.name }));\n} else {\n  // Modal might not use role=dialog\n  const form = document.querySelector('[class*=\"modal\"], [class*=\"dialog\"], form');\n  form ? form.innerHTML.substring(0, 1000) : 'no modal found';\n}\n"})

> eval({"code":"\n// Fill in the form fields\nconst modal = document.querySelector('[role=\"dialog\"]');\nconst inputs = Array.from(modal.querySelectorAll('input, textarea'));\n\n// Company Name\ninputs[0].value = 'MidWest Fasteners Corp';\ninputs[0].dispatchEvent(new Event('input', { bubbles: true }));\ninputs[0].dispatchEvent(new Event('change', { bubbles: true }));\n\n// Industry\ninputs[1].value = 'Industrials';\ninputs[1].dispatchEvent(new Event('input', { bubbles: true }));\ninputs[1].dispatchEvent(new Event('change', { bubbles: true }));\n\n// Geography\ninputs[2].value = 'Midwest, North America';\ninputs[2].dispatchEvent(new Event('input', { bubbles: true }));\ninputs[2].dispatchEvent(new Event('change', { bubbles: true }));\n\n// Deal Source\ninputs[3].value = 'Proprietary';\ninputs[3].dispatchEvent(new Event('input', { bubbles: true }));\ninputs[3].dispatchEvent(new Event('change', { bubbles: true }));\n\n// Revenue\ninputs[4].value = '48';\ninputs[4].dispatchEvent(new Event('input', { bubbles: true }));\ninputs[4].dispatchEvent(new Event('change', { bubbles: true }));\n\n// EBITDA\ninputs[5].value = '9.5';\ninputs[5].dispatchEvent(new Event('input', { bubbles: true }));\ninputs[5].dispatchEvent(new Event('change', { bubbles: true }));\n\n// EV\ninputs[6].value = '80';\ninputs[6].dispatchEvent(new Event('input', { bubbles: true }));\ninputs[6].dispatchEvent(new Event('change', { bubbles: true }));\n\n// Description\ninputs[7].value = 'Specialty fasteners manufacturer serving industrial OEM customers. Add-on acquisition target for Apex Capital Fund III industrials platform.';\ninputs[7].dispatchEvent(new Event('input', { bubbles: true }));\ninputs[7].dispatchEvent(new Event('change', { bubbles: true }));\n\n'Fields filled';\n"})
Now let me set the Deal Type to "Add-on":
> click({"text":"Add-on"})
There's an overlay blocking clicks. Let me use the Select element directly:
> eval({"code":"\n// Use the native select element for Deal Type\nconst modal = document.querySelector('[role=\"dialog\"]');\nconst select = modal.querySelector('select');\nif (select) {\n  select.value = 'Add-on';\n  select.dispatchEvent(new Event('change', { bubbles: true }));\n  'Set select to Add-on: ' + select.value;\n} else {\n  'no select found';\n}\n"})

> screenshot({})
"Add-on" is selected. Now let me click "Create Deal":
> eval({"code":"\n// Click Create Deal button\nconst modal = document.querySelector('[role=\"dialog\"]');\nconst buttons = Array.from(modal.querySelectorAll('button'));\nconst createBtn = buttons.find(b => b.innerText.trim() === 'Create Deal');\nif (createBtn) {\n  createBtn.click();\n  'Clicked Create Deal';\n} else {\n  'Button not found. Buttons: ' + buttons.map(b => b.innerText.trim()).join(', ');\n}\n"})

> wait({"ms":3000})

> screenshot({})
The modal is still open. The Company Name might be required and not getting set correctly via JS. Let me verify and try a different approach.
> eval({"code":"\nconst modal = document.querySelector('[role=\"dialog\"]');\nconst inputs = Array.from(modal.querySelectorAll('input, textarea'));\ninputs.map((i, idx) => ({ idx, placeholder: i.placeholder, value: i.value, type: i.type }));\n"})
The values were reset! React's controlled inputs need React synthetic events. Let me use the proper approach with `nativeInputValueSetter`:
> eval({"code":"\n// Use React's internal value setter\nconst nativeInputValueSetter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set;\nconst nativeTextAreaValueSetter = Object.getOwnPropertyDescriptor(window.HTMLTextAreaElement.prototype, 'value').set;\n\nconst modal = document.querySelector('[role=\"dialog\"]');\nconst inputs = Array.from(modal.querySelectorAll('input'));\nconst textarea = modal.querySelector('textarea');\n\nfunction setReactValue(el, value) {\n  const setter = el.tagName === 'TEXTAREA' ? nativeTextAreaValueSetter : nativeInputValueSetter;\n  setter.call(el, value);\n  el.dispatchEvent(new Event('input', { bubbles: true }));\n  el.dispatchEvent(new Event('change', { bubbles: true }));\n}\n\nsetReactValue(inputs[0], 'MidWest Fasteners Corp');\nsetReactValue(inputs[1], 'Industrials');\nsetReactValue(inputs[2], 'Midwest, North America');\nsetReactValue(inputs[3], 'Proprietary');\nsetReactValue(inputs[4], '48');\nsetReactValue(inputs[5], '9.5');\nsetReactValue(inputs[6], '80');\nif (textarea) setReactValue(textarea, 'Specialty fasteners manufacturer serving industrial OEM customers. Add-on acquisition target.');\n\n// Check values\ninputs.map((i, idx) => ({ idx, value: i.value }));\n"})

> screenshot({})
The values still seem not visible in the screenshot. Let me try clicking directly into the Company Name field and typing:
> eval({"code":"\n// Focus first input in dialog\nconst modal = document.querySelector('[role=\"dialog\"]');\nconst firstInput = modal.querySelector('input[placeholder=\"Target company\"]');\nfirstInput.focus();\nfirstInput.click();\n'focused';\n"})

> type({"selector":"[role=\"dialog\"] input[placeholder=\"Target company\"]","text":"MidWest Fasteners Corp"})

> type({"selector":"[role=\"dialog\"] input[placeholder=\"e.g. Healthcare\"]","text":"Industrials"})

> type({"selector":"[role=\"dialog\"] input[placeholder=\"e.g. North America\"]","text":"Midwest, North America"})

> type({"selector":"[role=\"dialog\"] input[placeholder=\"e.g. Proprietary\"]","text":"Proprietary"})

> type({"selector":"[role=\"dialog\"] input[type=\"number\"][placeholder=\"0\"]:nth-of-type(1)","text":"48"})

> eval({"code":"\n// Get all number inputs in modal\nconst modal = document.querySelector('[role=\"dialog\"]');\nconst numInputs = Array.from(modal.querySelectorAll('input[type=\"number\"]'));\nnumInputs.map((i, idx) => ({ idx, value: i.value, placeholder: i.placeholder }));\n"})

> eval({"code":"\n// Set number fields using React setter\nconst nativeInputValueSetter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set;\nconst modal = document.querySelector('[role=\"dialog\"]');\nconst numInputs = Array.from(modal.querySelectorAll('input[type=\"number\"]'));\n\nfunction setVal(el, val) {\n  nativeInputValueSetter.call(el, val);\n  el.dispatchEvent(new Event('input', { bubbles: true }));\n  el.dispatchEvent(new Event('change', { bubbles: true }));\n}\n\nsetVal(numInputs[0], '48000000');\nsetVal(numInputs[1], '9500000');\nsetVal(numInputs[2], '80000000');\n\nnumInputs.map(i => i.value);\n"})


---
Total time: 2026s | Turns: 80
Report: /Users/rayyanui/factoriq-insight-flow/qa-bot/reports/run-2026-03-18T14-27-32-742Z.md
Video: /Users/rayyanui/factoriq-insight-flow/qa-bot/videos
